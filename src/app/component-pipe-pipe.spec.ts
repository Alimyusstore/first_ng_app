import { ComponentPipePipe } from './component-pipe-pipe';

describe('ComponentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
